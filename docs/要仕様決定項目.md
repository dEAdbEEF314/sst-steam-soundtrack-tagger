# 要仕様決定項目

本ドキュメントは「実装済みまたは実運用中だが、仕様として未確定・未記載の項目」を管理します。
決定後は対応する設計ドキュメントへ移動し、本ドキュメントから削除してください。

---

## 未確定項目一覧

### [Scout] mode.upload_audio_files

| 項目 | 内容 |
|------|------|
| 現状 | `scout/config.yaml` と `scout/config.example.yaml` に `mode.upload_audio_files: true` が存在 |
| 実装 | `uploader.py` の `upload_app()` が `upload_audio` パラメータとして受け取り、S3 への音源アップロードを制御している |
| CLI 対応 | `--no-audio` フラグが `upload_audio=False` に対応 |
| 未確定点 | `mode.upload_audio_files` を config から読んで `upload_audio` を上書きする処理が `main.py` に実装されているか、または CLI フラグのみが制御手段か |
| 対応ドキュメント候補 | `docs/CONFIG_SPEC.md` の Scout → mode セクション |
| 担当アクション | `scout/src/main.py` を確認し、config から `upload_audio_files` を読んでいるか検証。読んでいれば CONFIG_SPEC の mode 節に追記する |

---

### [Core] Prefect 運用スクリプトの実動確認

| 項目 | 内容 |
|------|------|
| 現状 | `core/prefect/` に以下の PowerShell スクリプトが存在するが、実動作の確認が未実施 |
| スクリプト一覧 | `setup-work-pool.ps1` / `deploy-worker-flow.ps1` / `run-worker-deployment.ps1` |
| 未確定点 | 各スクリプトが現在の Prefect サーバー設定・環境変数・work pool 名と一致しているか |
| 影響範囲 | Phase 4 Integration テストの「Operational runbook」要件 |
| 担当アクション | 実環境で 3 スクリプトを順番に実行し、動作を確認する。エラーが出た場合はスクリプトを修正して PREFECT_FLOW.md の Run Checklist を更新する |

---

## 決定済み項目（移動済み）

（なし）
